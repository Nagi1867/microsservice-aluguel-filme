version: '3.4'

services:
  naming-server:
    image: nagi1867/naming-server:0.0.1-SNAPSHOT
    ports:
      - 8761:8761
    networks:
      - gabriel-network
networks:
  gabriel-network:
    driver: bridge